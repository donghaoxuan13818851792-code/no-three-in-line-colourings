# Q family

Profiles with exactly one size-22 colour class.

Expected subdirectories:

`Q01/` through `Q10/`.

The common outer object is a feasible size-22 anchor modulo square symmetry. Each subprofile must retain its own exact residual formulation and terminal evidence.
