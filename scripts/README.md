# Scripts

Small reproducibility, manifest, reconstruction and verification scripts belong here.

Production-scale computation should be packaged separately when its data volume would make ordinary Git history impractical.
